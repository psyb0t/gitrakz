.vscode
.log
__pycache__
coverage.out
coverage.html
coverage.txt
coverage_filtered.txt
build
CLAUDE.md
.env
research_files
# The dotted form is the one actually used; the bare name above never matched it.
.research_files
.backup
scripts/.post-update-temp
git-update.sh
.telemetry/
coverage-percent.txt
.testing/
.anustimes/
.secaudit/
.brutal_reviews/
.rule_audits/

# plan-and-delegate scaffolding — never commit
.plan-and-delegate/

# Frontend (web/) — installed deps + local vite state, never committed. The
# built dist/ that spa.go embeds lives under internal/pkg/services/http-server/
# and IS committed (it's what ships in the binary), so it's not covered here.
web/node_modules
web/.vite
.wave6/
